{
  "name": "async-task-runner",
  "version": "1.0.0",
  "description": "A simple Node.js project that demonstrates asynchronous control flow patterns (sequential, parallel, and parallelLimit) using callbacks, promises, and async/await.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test || node tests/run-all.js"
  },
  "bin": {
    "async-task-runner": "./cli.js",
    "atr": "./cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "minimist": "^1.2.8"
  }
}
